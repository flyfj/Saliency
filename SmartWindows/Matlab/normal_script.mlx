<!DOCTYPE FilterScript>
<FilterScript>
 <filter name="Compute normals for point sets">
  <Param name="K" tooltip="The number of neighbors used to estimate normals." value="10" description="Neighbour num" type="RichInt"/>
  <Param name="smoothIter" tooltip="The number of smoothing iteration done on the p used to estimate and propagate normals." value="3" description="Smooth Iteration" type="RichInt"/>
  <Param name="flipFlag" tooltip="If the 'viewpoint' (i.e. scanner position) is known, it can be used to disambiguate normals orientation, so that all the normals will be oriented in the same direction." value="true" description="Flip normals w.r.t. viewpoint" type="RichBool"/>
  <Param name="viewPos" x="0" tooltip="The viewpoint position can be set by hand (i.e. getting the current viewpoint) or it can be retrieved from mesh camera, if the viewpoint position is stored there." y="0" z="0" description="Viewpoint Pos." type="RichPoint3f"/>
 </filter>
</FilterScript>
